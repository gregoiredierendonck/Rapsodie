---
title: Les Concerts Rapsodie - En france ou à l'international, Rapsodie en concert
description: Rapsodie en concert - à Lille - en France - à l'international - le rap au-delà des frontières pour un live hors norme
keywords: rapsodie, rap, musique classique, concert
changefreq: "monthly"
priority: "0.8"
---

div.concerts-content

	div.container-xl.grid-table.image-background
		div.texte.h1-title
			h1 = data.concerts.title
		div.grid.grid-two.grid-cell.grid-border.element-remove
			div.grid.grid-one.grid-vertical
				= image_tag "#{data.concerts.imagecomposition1}", alt: "Rapsodie #{data.concerts.title}", class: 'columns-1'
			div.grid.grid-one
				= image_tag "#{data.concerts.imagecomposition2}", alt: "Rapsodie #{data.concerts.title}", class: 'columns-1'
		div.grid.grid-two.grid-cell.grid-border
			= image_tag data.concerts.imagecomposition3, alt: "data.concerts.title", class: 'columns-1'

	div.container-small
		p class="introduction-text" = data.concerts.introduction

	div.container-medium
		= link_to "http://www.youtube.com/watch?v=ZmuUYlZxZWw", class: 'videolink magnific-video-link' do
			= image_tag "#{data.concerts.videoimage}", alt: "Rapsodie #{data.concerts.videotitle}"

	div.container-full.concerts-list
		h2.visuallyhidden Liste des concerts
		div.container-small.text-center
			h3 International : les derniers concerts
		div.container-xl
			ul
				- data.concertslistinternational.each_with_index do |concert|
					li.grid.grid-six.image-background
						div.texte
							h4 = concert.lieu
							p.lieu = concert.title
							p.subline = concert.date
						div.image
							= image_tag "#{concert.image}", alt: "Les concerts Rapsodie - #{concert.title}"

	div.container-full.concerts-list
		div.container-small.text-center
			h3 France : les derniers concerts
		div.container-xl
			ul
				- data.concertslistfrance.each_with_index do |concert|
					li.grid.grid-six.image-background
						div.texte
							h4 = concert.lieu
							p.lieu = concert.title
							p.subline = concert.date
						div.image
							= image_tag "#{concert.image}", alt: "Les concerts Rapsodie - #{concert.title}"

	.container-small.calltoaction
		div.calltoaction-contenter
			div.calltoaction-content
				h2.visuallyhidden Contact
				p = data.settings.calltoaction
				= link_to 'Contactez-nous', "#{data.settings.site.url}/contact.html", :class => "changepage button button-primary"
